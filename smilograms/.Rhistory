setwd("~/Documents/ANSDU/Tinamous/smilograms/")
library(ggplot2)
data<-read.csv("smilogram-1000flank")
s<-qplot(data$distance_from_center,data$freq, colour=data$substitutions,
xlab = "Distance from center of alignment (bp)",
ylab = "Frequency of variant bases", main="UCEs 1000 bp flanking region") +
scale_colour_gradient2(high="tomato2", mid = "ivory" low ="steelblue2")+
s<-qplot(data$distance_from_center,data$freq, colour=data$substitutions,
xlab = "Distance from center of alignment (bp)",
ylab = "Frequency of variant bases", main="UCEs 1000 bp flanking region") +
scale_colour_gradient2(high="tomato2", mid = "ivory" low ="steelblue2",
s<-qplot(data$distance_from_center,data$freq, colour=data$substitutions,
xlab = "Distance from center of alignment (bp)",
ylab = "Frequency of variant bases", main="UCEs 1000 bp flanking region") +
scale_colour_gradient2(high="tomato2", mid = "ivory", low ="steelblue2",
midpoint = max(substitutions)/2, limit = c(min(substitutions),max(substitutions)), space = "Lab",
name="# substitutions")+
labs(colour='# substitutions')
s<-qplot(data$distance_from_center,data$freq, colour=data$substitutions,
xlab = "Distance from center of alignment (bp)",
ylab = "Frequency of variant bases", main="UCEs 1000 bp flanking region") +
scale_fill_gradient2(low = "steelblue3", high = "tomato3", mid = "ivory",
midpoint = max(RF)/2, limit = c(min(RF),max(RF)), space = "Lab",
name="RF Distance") +
labs(colour='# substitutions')
s<-qplot(data$distance_from_center,data$freq, colour=data$substitutions,
xlab = "Distance from center of alignment (bp)",
ylab = "Frequency of variant bases", main="UCEs 1000 bp flanking region") +
scale_fill_gradient2(low = "steelblue3", high = "tomato3", mid = "ivory",
midpoint = max(data$substitutions)/2, limit = c(min(data$substitutions),max(data$substitutions)), space = "Lab",
name="# substitutions") #+
s<-qplot(data$distance_from_center,data$freq, colour=data$substitutions,
xlab = "Distance from center of alignment (bp)",
ylab = "Frequency of variant bases", main="UCEs 1000 bp flanking region") +
scale_fill_gradient2(low = "steelblue3", high = "tomato3", mid = "ivory",
midpoint = max(data$substitutions)/2, limit = c(min(data$substitutions),max(data$substitutions)), space = "Lab",
name="# substitutions") #+
setwd("~/Documents/ANSDU/Tinamous/smilograms/")
library(ggplot2)
data<-read.csv("smilogram-1000flank")
s<-qplot(data$distance_from_center,data$freq, colour=data$substitutions,
xlab = "Distance from center of alignment (bp)",
ylab = "Frequency of variant bases", main="UCEs 1000 bp flanking region") +
scale_fill_gradient2(low = "steelblue3", high = "tomato3", mid = "ivory",
midpoint = max(data$substitutions)/2, limit = c(min(data$substitutions),max(data$substitutions)), space = "Lab",
name="# substitutions") #+
labs(colour='# substitutions')
s<-qplot(data$distance_from_center,data$freq, colour=data$substitutions,
xlab = "Distance from center of alignment (bp)",
ylab = "Frequency of variant bases", main="UCEs 1000 bp flanking region") +
scale_fill_gradient2(low = "steelblue3", high = "tomato3", mid = "ivory",
midpoint = max(data$substitutions)/2, limit = c(min(data$substitutions),max(data$substitutions)), space = "Lab",
name="# substitutions") +
labs(colour='# substitutions')
s
s<-qplot(data$distance_from_center,data$freq, colour=data$substitutions,
xlab = "Distance from center of alignment (bp)",
ylab = "Frequency of variant bases", main="UCEs 1000 bp flanking region") +
scale_fill_gradient2(low = "steelblue3", high = "tomato3", mid = "ivory",
midpoint = max(data$substitutions)/2, limit = c(min(data$substitutions),max(data$substitutions)), space = "Lab",
name="# substitutions") #+
min(data$substitutions)
max(data$substitutions)
s<-qplot(data$distance_from_center,data$freq, colour=data$substitutions,
xlab = "Distance from center of alignment (bp)",
ylab = "Frequency of variant bases", main="UCEs 1000 bp flanking region") +
scale_fill_gradient2(low = "steelblue3", high = "tomato3", mid = "ivory",
midpoint = max(data$substitutions)/2, limit = c(min(data$substitutions),max(data$substitutions)), space = "",
name="# substitutions") #+
s
